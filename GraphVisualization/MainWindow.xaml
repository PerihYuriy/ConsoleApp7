<Window x:Class="GraphVisualization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GraphVisualization"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Left side: controls + canvas -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10">
                <TextBlock Text="Start Vertex:" VerticalAlignment="Center"/>
                <TextBox x:Name="StartVertexBox" Width="40" Margin="5,0"/>
                <Button x:Name="DfsButton" Content="Run DFS" Click="DfsButton_Click" Margin="5,0"/>
                <Button x:Name="BfsButton" Content="Run BFS" Click="BfsButton_Click" Margin="5,0"/>
            </StackPanel>
            <Canvas x:Name="GraphCanvas" Background="White"/>
        </DockPanel>

        <!-- Right side: logging panel -->
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogBox"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Hidden"
                 Background="Black"
                 Foreground="LightGreen"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
